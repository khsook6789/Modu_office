# 🚀 Swagger & Test Guide (백엔드 보증서)

이 문서는 백엔드 API를 이해하고, **테스트 코드가 무엇을 보장하는지** 한눈에 확인할 수 있도록 돕기 위해 작성되었습니다.

---

## 1. 🔗 Swagger UI 활용 가이드 (100% 활용하기)

이제 별도의 **Postman 설치나 API Collection 공유가 필요 없습니다.** Swagger UI를 통해 브라우저에서 즉시 모든 API를 테스트하고, 최신 버전의 응답 규격을 확인할 수 있습니다.

### 🔌 A. 접속 및 초기 설정 (인증 방법)

- **주소**: [http://localhost:8080/swagger-ui/index.html](http://localhost:8080/swagger-ui/index.html)
- **자물쇠가 열려 있다면(Unauthorized)?** 아래 순서대로 토큰을 먼저 얻어야 합니다.

#### 🔐 [Step-by-Step] 인증 토큰 적용하기

1. **회원가입**: `user-auth-controller` -> `POST /api/auth/user/signup` 에서 **'Try it out'** 후 계정 생성
2. **로그인**: `POST /api/auth/user/login` 에서 **'Try it out'** 후 계정 정보 입력 및 `Execute`
3. **토큰 복사**: 응답 바디(`Response Body`)에 있는 `"accessToken": "..."` 값 중 **따옴표를 제외한 문자열만 복사**
4. **인증 적용**:
   - 페이지 최상단 우측의 **'Authorize' (자물쇠 모양)** 버튼 클릭
   - **Value** 칸에 `Bearer ` (Bearer 한 칸 띄우고) 뒤에 복사한 토큰 붙여넣기  
     _(예: `Bearer eyJhbG...`)_
   - `Authorize` 버튼 클릭 후 `Close`
5. **확인**: 자물쇠가 **닫힌 모양**으로 바뀌었다면 이제 모든 API 호출 시 인증이 자동으로 포함됩니다!

### 🧪 B. API 직접 테스트 (Try it out)

1. 원하는 API 블록을 클릭하여 펼칩니다.
2. 오른쪽 상단 **'Try it out'** 버튼을 클릭합니다.
3. 파라미터나 Request Body(JSON)를 수정합니다.
4. **'Execute'** 버튼(파란색)을 클릭합니다.
5. **Responses** 구역에서 `Curl` 명령어, `Request URL`, 그리고 최종 `Response Body`를 확인합니다.

### 📋 C. Schema 및 모델 확인

- 각 API 응답 아래의 **'Schema'**를 클릭하면 각 필드의 타입(String, Number 등)과 필수 여부를 확인할 수 있습니다.

---

### 🎭 D. 권한별 테스트 안내 (Role-based Testing)

Swagger는 실제 서버 권한을 그대로 따릅니다. 어떤 계정으로 로그인했느냐에 따라 호출 가능한 API가 달라집니다.

| 역할 (Role)          | 전용 로그인 API           | 테스트 가능 범위 (예시)            |
| :------------------- | :------------------------ | :--------------------------------- |
| **일반 고객 (USER)** | `/api/auth/user/login`    | 내 예약, 리뷰 작성, 공간 검색      |
| **운영자 (MANAGER)** | `/api/auth/manager/login` | 지점/회의실 관리, 예약 승인/거절   |
| **관리자 (ADMIN)**   | `/api/auth/admin/login`   | 모든 리소스 관리, 시스템 로그 조회 |

> [!TIP]
> **권한 전환 방법**: 다른 역할을 테스트하고 싶다면, 최상단 **'Authorize'** 버튼을 다시 눌러 기존 토큰을 지우고(Logout), 새로운 역할의 토큰을 `Bearer ` 뒤에 넣어주시면 됩니다.

---

## 2. ✅ Test Catalog: 백엔드가 보장하는 비즈니스 규칙

우리 프로젝트의 모든 컨트롤러 테스트 코드가 보장하는 구체적인 비즈니스 명세입니다.

### 🔐 1. 인증 및 권한 (Security & Auth)

| 테스트 파일                   | 보장하는 핵심 비즈니스 규칙                                                                                              |
| :---------------------------- | :----------------------------------------------------------------------------------------------------------------------- |
| **UserAuthControllerTest**    | 일반 사용자 회원가입 시 중복 검증 및 비밀번호 암호화 저장 보장, 로그인 성공 시 유효한 JWT 토큰(Access/Refresh) 발급 확인 |
| **ManagerAuthControllerTest** | 운영자(Manager) 전용 로그인 프로세스와 권한 부여 로직 검증                                                               |
| **AdminAuthControllerTest**   | 플랫폼 관리자(Admin)의 보안 강화된 로그인 및 토큰 갱신 로직 보장                                                         |

### 👤 2. 사용자 정보 관리 (User)

| 테스트 파일                 | 보장하는 핵심 비즈니스 규칙                                                                                       |
| :-------------------------- | :---------------------------------------------------------------------------------------------------------------- |
| **UserControllerTest**      | 내 프로필 조회 시 민감 정보 제외 보장, **비밀번호 변경 시 8자 이상/기존 비번 일치 여부** 등 강력한 보안 정책 적용 |
| **AdminUserControllerTest** | 관리자에 의한 사용자 상태 제어(정지/복구) 시 비즈니스 로직의 무결성 보장                                          |

### 🏢 3. 지점 및 공간 (Office & Room)

| 테스트 파일                    | 보장하는 핵심 비즈니스 규칙                                                                                                                                                                                                          |
| :----------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **OfficeControllerTest**       | 지점 등록 시 위치 정보(경위도) 및 필수 운영 시간 데이터 유효성 검증 보장, 관리자 권한별 접근 제어 확인                                                                                                                               |
| **RoomControllerTest**         | 회의실별 수용 인원, 가격, **정비 시간(Buffer Time, 최대 120분)** 등 데이터 매핑 및 유효성 검증 보장, **다중 이미지 등록 및 배너 자동 선정 로직(displayOrder)**의 정확성 검증, 회의실 상태(AVAILABLE/INACTIVE) 변경 시 즉각 반영 확인 |
| **FacilityControllerTest**     | 각 지점에 부합하는 시설 정보(WiFi, 주차 등)의 정확한 리스트 제공 보장                                                                                                                                                                |
| **RoomFavoriteControllerTest** | 사용자별 공간 '찜하기' 기능의 중복 추가 방지 및 정확한 내역 조회 보장                                                                                                                                                                |
| **MyOfficesApiTest**           | 운영자가 본인이 관리하는 지점 목록을 조회할 때의 정확한 필터링 보장                                                                                                                                                                  |

### 📅 4. 예약 및 리뷰 (Reservation & Review)

| 테스트 파일                        | 보장하는 핵심 비즈니스 규칙                                                                                                                                                               |
| :--------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ReservationControllerTest**      | **중복 예약(동일 시간대) 원천 차단 및 정비 시간(Buffer Time) 자동 확보**, 예약 시 가격 자동 계산, **취소 시점별 환불 금액 미리보기(`refund-preview`) 및 본인 예약만 취소 가능 정책** 확인 |
| **AdminReservationControllerTest** | 관리자가 비정상 예약을 **강제 취소(Force Cancel)하고 환불 비율을 수동 조정**할 때의 데이터 일관성 및 실시간 알림 전송 보장                                                                |
| **ReviewControllerTest**           | **반드시 이용 완료(COMPLETED)된 예약에 대해서만 리뷰 작성 가능** 확인, 별점 범위(1~5점) 및 작성자 본인 수정 권한 보장                                                                     |

### 📜 5. 시스템 및 로그 (System & Logs)

| 테스트 파일                    | 보장하는 핵심 비즈니스 규칙                                                                         |
| :----------------------------- | :-------------------------------------------------------------------------------------------------- |
| **AuditLogControllerTest**     | 중요 데이터(예약 등) 변경 시 생성되는 감사 로그의 정확성 및 JSONB 정밀 검색 기능 보장               |
| **NotificationControllerTest** | **개인별 실시간 알림(SSE) 및 방별 캘린더 구독(SSE)** 프로세스 검증, 오래된 알림 자동 삭제 로직 보장 |

### 🔧 6. 시설 신고 (Facility Report)

| 테스트 파일                   | 보장하는 핵심 비즈니스 규칙                                                                                                                                                                            |
| :---------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **FacilityReportServiceTest** | **예약 기반 신고 접수(강력한 IDOR 방어)** 및 예약 시작 전 신고 차단, **미처리 신고 중복 접수(409) 차단**, `IN_PROGRESS` 전환 시 **시설 자동 비활성화** 로직 보장, 상태 역전(RESOLVED→IN_PROGRESS) 차단 |

### 💳 7. 결제 (Payment)

| 테스트 파일               | 보장하는 핵심 비즈니스 규칙                                                                                                                       |
| :------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------ |
| **PaymentControllerTest** | **토스페이먼츠 결제 승인(`confirm`) API**의 입력값 검증 및 성공 응답 구조 보장, **예약별 결제 내역 조회 API**의 사용자 권한 및 데이터 정확성 검증 |

---

## 3. 📡 실시간 통신 (SSE) 테스트 안내

우리 프로젝트는 **WebSocket 대신 SSE(Server-Sent Events)**를 사용합니다. Swagger UI는 실시간 스트리밍(SSE) 연결을 시각적으로 유지해 주지 않으므로, 테스트 시 아래 사항을 참고하세요.

1. **일반 알림 목록 (`/api/notifications`)**:
   - Swagger에서 'Execute' 시 현재 DB에 쌓인 알림을 JSON으로 즉시 확인할 수 있습니다.
2. **실시간 구독 (`/api/notifications/subscribe`)**:
   - 이 API를 Swagger에서 호출하면 연결이 계속 유지(Loading 상태)됩니다.
   - 웹 브라우저 주소창에 직접 입력하여 테스트하거나, 프론트엔드 코드(EventSource)를 통해 연결을 확인해야 합니다.
3. **캘린더 동기화 (`/api/notifications/subscribe/rooms/{roomId}`)**:
   - 특정 회의실 페이지 진입 시 호출되는 엔드포인트입니다.

---

## 3. 💬 공통 응답 규격 (Common Response)

모든 API는 아래 구조를 100% 준수하므로, 에러 핸들링 시 참고하세요.

```json
{
  "status": "SUCCESS | ERROR", // 처리 결과 상태
  "code": "200 | 400 | 500",   // HTTP 상태 코드 또는 비즈니스 에러 코드
  "message": "사용자 메시지",     // UI(Toast/Alert)에 즉시 노출 가능한 한글 메시지
  "data": { ... }              // 성공 시 데이터 (없으면 null)
}
```
